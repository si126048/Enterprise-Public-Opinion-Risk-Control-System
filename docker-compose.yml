version: "3.8"

services:
  app:
    build: .
    container_name: opinion-monitor
    ports:
      - "8000:8000"
    volumes:
      - app-data:/app/data
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

volumes:
  app-data:
